# SAP Business ByDesign Integration Configuration
# Copy this file to .env and fill in your SAP credentials

# SAP ByDesign Base URL (Test Tenant)
# Format: https://[tenant-id].sapbydesign.com
# Example: https://test123456.sapbydesign.com
SAP_BYD_BASE_URL=

# SAP ByDesign Tenant ID
# This is usually the subdomain of your SAP URL
# Example: test123456
SAP_BYD_TENANT_ID=

# Authentication Method 1: Basic Authentication
# Use this if you don't have OAuth2 configured
SAP_BYD_USER=
SAP_BYD_PASSWORD=

# Authentication Method 2: OAuth2 (Recommended for Production)
# Leave empty if using Basic Auth
SAP_BYD_CLIENT_ID=
SAP_BYD_CLIENT_SECRET=

# Optional: Default warehouse location for inventory
# If not specified, defaults to 'MAIN'
SAP_BYD_DEFAULT_WAREHOUSE=MAIN

# Optional: Enable SAP integration (true/false)
# Set to false to disable SAP pushes while keeping config
SAP_INTEGRATION_ENABLED=true

# Optional: SAP Integration Mode (test/production)
# In test mode, logs are verbose and errors are non-fatal
SAP_INTEGRATION_MODE=test

# Notes:
# 1. For initial testing, use your SAP test tenant credentials
# 2. Basic Auth is simpler to start with
# 3. Ensure your SAP user has permissions for:
#    - Material Master Data read
#    - Inventory/Goods Movement create
#    - Stock Overview read
# 4. Test connection with: GET /api/sap-test/connection
# 5. Test with sample product: POST /api/sap-test/push-sample